<header id="header" class="bg-success">
    <div class="container">
        <h1 class="d-none">La Nîmes'alerie</h1>
    
        <nav class="navbar navbar-expand-lg navbar-dark bg-success">

        <a class="navbar-brand" href="{{path('app_homepage')}}"><img src="{{asset('build/images/logo.png')}}" alt="Logo"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
          aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        {# If conditon if user is connected or anonymous #}
        {% if app.user %}
            <div>
                <a href="{{path('app_user')}}" class="text-warning me-5">{{app.user.firstname}} {{app.user.lastname}}</a>
                <a href="{{path('app_logout')}}" class="pipe text-warning">Deconnexion</a>
            </div> 
        {% else %}
            <div>
                <a href="{{path('app_login')}}" class="text-warning me-5">Connexion</a>
                <a href="{{path('app_register')}}" class="text-warning">Inscription</a>
            </div>
        {% endif %}

        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{path('app_homepage_admin')}}">Vue Admin</a>
        {% endif %}

        </nav>
        <nav class="navbar navbar-expand-lg navbar-dark bg-success ">
            <div class="collapse navbar-collapse d-flex justify-content-around " id="navbarNav">
                <ul class="navbar-nav ml-auto ">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{path('app_homepage')}}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Chiens</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Chats</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link " href="#">Poissons</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link " href="#">Vivariums</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link " href="#">Basse-cour</a>
                    </li>
                </ul>
            </div>
        </nav>
    </div
</header>