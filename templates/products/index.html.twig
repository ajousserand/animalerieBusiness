{% extends 'base-front.html.twig' %}

{% block title %}Nos produits{% endblock %}

{% block body %}
<ul>
{% for product in pagination %}
    <a href="{{path('app_show_product',{id:product.id})}}">
        <li>{{product.label}}</li>
    </a>
{% endfor %}
</ul>

{{ knp_pagination_render(pagination) }}
{% endblock %}
